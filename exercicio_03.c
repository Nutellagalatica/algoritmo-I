/*
as variaveis int a, b e c foram usadas para armazenar valores inteiros

a variavel a recebeu o valor 1
a variavel b recebeu o valor 2
a variavel c recebeu o valor 3

o printf foi usado para mostrar na tela o valor da variavel a
o printf foi usado para mostrar na tela o valor da variavel b
o printf foi usado para mostrar na tela o valor da variavel c

o \n foi usado para pular uma linha depois de mostrar cada valor

o return 0 foi usado para indicar que o programa terminou corretamente
*/
#include <stdio.h>
 int main(){
   int a;
   int b;
   int c;

   a=1;
   b=2;
   c=3;
   printf("%d \n", a);
   printf("%d \n", b);
   printf("%d \n", c);
 return 0;
 }
